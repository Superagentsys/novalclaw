# 修改记录

本文件从 v0.1.11.0 开始维护，更早版本的变更请查阅 `git log` 与各版本 tag。

## 未发布

### 修复

- **代码搜索不再依赖系统安装 `ripgrep` / `grep`。** `content_search` 改用内置正则搜索，并统一排除虚拟环境、依赖缓存、版本库数据和根目录 `.gitignore` 命中的路径。
- **目录和文件读取加入上下文保护。** `file_list` 默认最多返回 200 项并支持 `offset` 分页，`file_read` 默认最多返回 400 行并给出下一页参数；两者不再把 `.venv`、`__pycache__`、`.pytest_cache` 等噪音大量注入模型上下文。
- **委派智能体继承父任务的实际 Workspace。** `delegate` 请求及 Gateway 元数据现在显式携带父任务 Workspace，避免子智能体回退到自身或全局目录后在错误项目中搜索、修改。
- 保留模型流式响应不完整时“不自动重放已开始工具调用”的安全策略，避免写文件、Shell 或 Git 操作因重试重复执行。

## v0.1.11.0

本次发布集中修复记忆模块的持久化问题：此前断开重连后记忆会丢失，根因分布在工具装配、后端初始化、会话存储三个层面。

### 修复

- **长期记忆此前完全未接通。** `GatewayRuntime::new()` 把记忆后端硬编码为进程内存储，`memory.backend` 配置被忽略，进程退出即丢失全部记忆；负责按配置构建后端的 `refresh_memory_from_config()` 从未被调用。现在构造时即按配置建立持久化后端。
- **`memory_store` / `memory_recall` 未进入 Agent 工具集**，模型此前没有任何写入长期记忆的手段。两者已加入默认工具集，委派 Agent 的 `allowed_tools` 白名单仍然生效。
- **配置变更后记忆后端不会跟随切换。** 首次安装向导在网关运行时构造之后才写入 workspace，导致该次会话始终使用进程内存储。现在 `set_config` 会在 backend、db_path、workspace_dir 或 embedding 配置发生变化时重建后端。
- **会话历史文件损坏时静默清空。** 此前只要 JSON 有一处解析失败，整个文件即被改名搬走并静默返回空历史，用户感知为"记忆无故消失"。现在会逐条抢救可解析的会话记录，仅在完全无法解析时整体备份，并把恢复结果（保留数、丢弃数、备份路径）作为执行步骤回传前端。
- **会话历史保留时长从 24 小时改为 30 天**，隔夜清空历史同样会被误认为记忆丢失。`session_ttl_secs = 0` 现在表示永不过期。
- **历史超限时不再连同 system 提示词一起截断。** 兜底截断会保留开头的 system 消息与对话摘要。
- **`memory_store` 加入默认自动批准列表**（`memory_recall` 原已在列）。默认自主级别为 `supervised`，否则每记录一条事实都会触发一次人工批准，长期记忆实际不可用。

### 变更

- **记忆后端改为真正的 SQLite 实现。** 配置项 `backend = "sqlite"` 此前实际映射到 JSON 文件存储，每写一条记忆都要全量序列化覆写整个库。新实现按条 upsert，并启用 WAL 让桌面端读取不阻塞网关写入。`backend = "json"` 保留可用。
- **`compact_context` 配置项开始生效。** 该项默认为 true，但此前从未被读取，历史超限时直接丢弃最旧的消息。现在会把较早的对话交给模型压缩为摘要，保留引导提示词与最近一半对话；摘要调用失败则退化为保留 system 消息的截断，不会阻塞当轮对话。重复压缩时旧摘要会被折叠进新摘要，不会无限堆积。

### 新增

- **语义记忆检索。** 新增 OpenAI 兼容的 embedding 客户端（适用于 OpenAI、DeepSeek、Qwen、豆包、Ollama 等），以及关键词检索与向量相似度的倒数排名融合。仅在配置 `memory.embedding.provider` 后启用；embedding 请求失败会自动降级为纯关键词检索。向量存放在记忆数据库的 BLOB 列中，无需额外部署向量服务，单次检索最多扫描 5000 条向量。

### 已知限制

- `memory.qdrant_url` 配置项仍未接通，向量存储目前只使用本地 SQLite。
- 启用 `memory.embedding` 后，每轮对话会额外产生一次 embedding 请求，需评估调用成本。
